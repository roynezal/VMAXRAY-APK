name: Build VMAXRAY APK        (no indent)
                                (empty line)
on:                             (no indent)
  push:                         (2 spaces)
    branches: [ main, master ]  (4 spaces)
  workflow_dispatch:            (2 spaces)
                                (empty line)
jobs:                           (no indent)
  build:                        (2 spaces)
    runs-on: ubuntu-latest      (4 spaces)
                                (empty line)
    steps:                      (4 spaces)
      - name: Checkout repo     (6 spaces)
        uses: actions/checkout@v3 (8 spaces)          path: project/vmaxray-android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
